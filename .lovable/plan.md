

# Actualizar Email del Dr. Salazar

## Cambio
Reemplazar `contacto@drjacintosalazar.com` por `jacinto_salazar1958@hotmail.com` en 3 archivos:

1. **`src/components/landing/Contact.tsx`** — campo `details` y `action` (mailto)
2. **`src/components/landing/Footer.tsx`** — texto visible del email
3. **`src/pages/PrivacyPolicy.tsx`** — sección de contacto

